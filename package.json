{
  "name": "aviutl2-plugins",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run postbuild",
    "postbuild": "node scripts/postbuild.js && npm run generate-sitemap",
    "generate-sitemap": "node scripts/generate-sitemap.js",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "dependencies": {
    "microcms-js-sdk": "^3.2.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.3"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "sitemap": "^8.0.0",
    "vite": "^5.0.12"
  }
}
